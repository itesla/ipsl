language: bash
python:
  - "2.7"

services: docker

install:
  - docker pull smartslab/openipsl

script:
  - docker run -i -t -v $(pwd):/OpenIPSL smartslab/openipsl -u smartslab -H sh -c "python /OpenIPSL/CI/syntaxCheck.py"
